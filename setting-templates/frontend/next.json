{
  "stack": "next",
  "description": "Next.js (App Router) projects. Same plugins as React - both are frontend/TS - inherited via `extends`; no Next.js-only plugin exists in the official catalog beyond that shared set.",
  "extends": ["frontend"],
  "merge": {},
  "plugins": []
}
