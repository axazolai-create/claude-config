{
  "stack": "react",
  "description": "React frontend projects (Vite or CRA-style, non-Next). Same plugins as Next.js - both are frontend/TS - inherited via `extends`; no React-only plugin exists in the official catalog beyond that shared set.",
  "extends": ["frontend"],
  "merge": {},
  "plugins": []
}
