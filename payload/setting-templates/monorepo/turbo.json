{
  "stack": "turbo",
  "description": "Turborepo monorepos (detected via turbo.json at repo root).",
  "merge": {
    "enabledPlugins": {
      "turborepo@pleaseai": true
    }
  },
  "plugins": [
    {
      "id": "turborepo@pleaseai",
      "description": "Third-party plugin (github.com/pleaseai/claude-code-plugins, MIT) - triggers on turbo.json, task pipelines, dependsOn, caching, --filter/--affected, CI optimization. Modest adoption, only dedicated Turborepo plugin found. Free; prefer local/self-hosted caching over its optional paid remote-cache guidance.",
      "check": {
        "installed_file": "~/.claude/plugins/installed_plugins.json",
        "cmd": "claude plugin list",
        "bash": "jq -e '.plugins[\"turborepo@pleaseai\"]' ~/.claude/plugins/installed_plugins.json >/dev/null 2>&1 && echo INSTALLED || echo MISSING",
        "slash": "/plugin list"
      },
      "install": {
        "marketplace_add": {
          "cmd": "claude plugin marketplace add https://github.com/pleaseai/claude-code-plugins",
          "slash": "/plugin marketplace add https://github.com/pleaseai/claude-code-plugins"
        },
        "cmd": "claude plugin install turborepo@pleaseai --scope project",
        "bash": "claude plugin install turborepo@pleaseai --scope project",
        "slash": "/plugin install turborepo@pleaseai"
      }
    }
  ],
  "_notes": [
    "The turborepo@pleaseai plugin above is the only dedicated Turborepo plugin; there is also a first-party Turborepo Agent Skill (npx skills add) if you want it.",
    "KNOWN ISSUE (2026-07): marketplace_add above uses HTTPS, but pleaseai/claude-code-plugins' own .gitmodules pins its submodules (code-review, firebase, flutter, grafana, nanobanana, postgres, security, spec-kit - none needed by turborepo) to git@github.com: SSH URLs, so the marketplace add can still fail with an SSH host-key/permission error on machines without GitHub SSH set up. Fix on the affected machine: git config --global url.\"https://github.com/\".insteadOf \"git@github.com:\" - see setting-templates/README.md for the full writeup."
  ]
}
