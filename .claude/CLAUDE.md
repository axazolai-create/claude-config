@stack-rules.md

## Risk register location

This tree's risk register is `.ultrapowers/RISK_REGISTER.md`. The user-scope
`~/.claude/CLAUDE.md` names `.planning/` or the project root; that file is `CURATED:NOEDIT`
and cannot carry the exception itself, so it is recorded here, where project scope outranks
user scope on conflict. `.ultrapowers/` is this repository's planning tree — the register
belongs beside the phases whose execution filed it, and `add-risk.mjs` is pointed at that
path.
