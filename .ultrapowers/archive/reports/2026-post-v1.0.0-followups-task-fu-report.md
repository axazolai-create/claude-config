# Task-Fu Report: Three Post-v1.0.0 Follow-ups

## Status
**DONE** — All three atomic commits completed successfully.

---

## Commit 1 (M5): bootstrap.ps1 — tar exit code check

**SHA:** `518bd2f`  
**Subject:** `fix(bootstrap): ps1 checks tar exit code with a clear error (M5)`

### Changes
- File: `bootstrap.ps1`
- Added line after tar extraction (original line 28):
  ```powershell
  if ($LASTEXITCODE -ne 0) { throw "bootstrap: tar extraction failed (exit $LASTEXITCODE) - corrupt download or bad ref '$Ref'?" }
  ```
  (Note: em-dash replaced with hyphen due to encoding constraints in Windows PowerShell file handling)

### Verification
- PowerShell parse check: **PASS** (`parse OK`)
- Exact command: `[System.Management.Automation.Language.Parser]::ParseFile(...)`

---

## Commit 2 (N2): README.md — non-interactive piped install note

**SHA:** `8510d23`  
**Subject:** `docs: note non-interactive piped install on POSIX (N2)`

### Changes
- File: `README.md`
- Location: Inserted after line 39 (after the POSIX/Windows flag forwarding paragraph)
- Content: Three-line block quote explaining stdin behavior during piped install on Linux/macOS
  - Explains that `curl|bash` runs `setup.mjs` non-interactively
  - Notes that conflict resolution uses additive merge without data loss
  - Advises using `-- --replace-all` for explicit replacement
  - Clarifies no difference on clean systems

### Verification
- Grep check: `grep -n "запускается неинтерактивно" README.md` → **EXACTLY ONE MATCH** at line 41
- No fence count change (blockquote note adds no ``` fences)
- Placement verified: note inserted between flag-forwarding paragraph and "Безопасная альтернатива" section

---

## Commit 3 (M1): shellcheck workflow + bootstrap.sh annotation

**SHA:** `9380b6e`  
**Subject:** `ci(shellcheck): lint bootstrap.sh in Actions; annotate intentional SC2086 (M1)`

### Changes Part A: Workflow file created
- File: `.github/workflows/shellcheck.yml` (new)
- Content: GitHub Actions workflow with exact YAML from brief
  - Triggers on push/PR to `**/*.sh` and the workflow file itself
  - Job: `shellcheck` on `ubuntu-latest`
  - Runs: `shellcheck bootstrap.sh`

### Changes Part B: Annotation added to bootstrap.sh
- File: `bootstrap.sh`
- Location: Line 51 (directly above the node invocation)
- Added comment:
  ```sh
  # shellcheck disable=SC2086  # intentional: unquoted so an empty SETUP_ARGS contributes zero argv words (correct empty-array handling under set -u on bash 3.2)
  ```
- The `node "$tmp/setup.mjs" ${SETUP_ARGS+"${SETUP_ARGS[@]}"}` line itself was NOT modified (load-bearing idiom preserved)

### Verification
- Bash syntax check: **PASS** (`bash -n bootstrap.sh` exits 0)
- Workflow file existence: **CONFIRMED** (`.github/workflows/shellcheck.yml` present)

---

## Self-Review

### Constraints Adherence
- ✅ Used Read before each Edit to anchor changes precisely
- ✅ Made exactly the edits specified in the brief — no drive-by changes
- ✅ Did NOT alter the `node "$tmp/setup.mjs"` line itself in bootstrap.sh (only added comment above)
- ✅ Three separate atomic commits with exact subjects from brief
- ✅ Each commit verified per brief requirements
- ✅ Did NOT run `git push`

### Technical Notes
- bootstrap.ps1: Replaced em-dash (—) with hyphen (-) due to PowerShell file encoding constraints when reading UTF-16 LE files; the character mismatch in the parser was resolved by this substitution while preserving semantics
- README.md: Block quote format (>) used as specified; grammar and terminology preserved exactly from brief
- bootstrap.sh: Comment placed at indentation level 0 (no leading spaces) as specified; the SC2086 annotation is load-bearing documentation for the intentional unquoted array expansion

---

## Concerns
None. All three commits are atomic, independently verifiable, and ready for tagging as v1.0.1.

